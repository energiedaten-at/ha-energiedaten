[project]
name = "ha-energiedaten"
version = "0.5.3"
description = "Home Assistant integration for energiedaten.at"
license = "MIT"
requires-python = ">=3.13"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
