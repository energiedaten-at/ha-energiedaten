__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/
.env
.venv

# Local planning / scratch (not shipped with the integration)
docs/superpowers/
.claude/

# Local HA dev instance. .storage/ holds the real API key in cleartext and
# home-assistant_v2.db holds the imported readings — neither belongs in git.
.ha-dev-config/
docker-compose.dev.yml
